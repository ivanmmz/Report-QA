# 202601.pdf 数据对比报告 (Comparison Report)

本报告对比了直接解析 `202601.pdf` 得到的所有真实数据，与通过 RAG 系统调用 App 的 AI Copilot 获取的数据，并对两者之间的巨大差异进行了深度的技术原因分析。

---

## 1. 真实数据 vs Copilot 数据对比

### 📊 真实数据（直接从 `202601.pdf` 中解析）
以下是 `202601.pdf`（2026年1月运行效率报告）中包含的全部真实监控和运行指标：

| 监控类别 | 指标名称 | 数值 |
| :--- | :--- | :--- |
| **基础信息** | 月份 (Month) | **Jan/2026** |
| | 水耗 (Usage Water) | **7,140.10 m³** |
| | 系统运行率 (System Uptime) | **100.00%** |
| **用电指标** | 进线1最大功率 (Feeder 1 kW Max) | **1,273.70 kW** |
| | 进线1总电量 (Feeder 1 kWh Total) | **352,000.50 kWh** |
| | 进线2最大功率 (Feeder 2 kW Max) | **1,426.37 kW** |
| | 进线2总电量 (Feeder 2 kWh Total) | **426,115.00 kWh** |
| | 进线合并最大功率 (Feeder Max kW) | **2,063.40 kW** |
| | 进线总耗电量 (Feeder Total kWh) | **778,115.50 kWh** |
| **冷机指标** | 冷量最大值 (RT Max) | **3,392.64 RT** |
| | 冷量中位数 (RT Median) | **1,629.86 RT** |
| | 冷量月总和 (RT Sum of Month) | **1,321,778.52 RT** |
| **温度指标** | Campus Heart CHWST (Avg) | **6.81 ℃** |
| | Campus Heart CHWRT (Avg) | **10.02 ℃** |
| | Campus Court CHWST (Avg) | **6.79 ℃** |
| | Campus Court CHWRT (Avg) | **11.91 ℃** |
| **冷量产出** | Campus Heart (RTh) | **577,670.62 RTh** |
| | Campus Court (RTh) | **715,877.65 RTh** |
| | SIT Owner / SIT Tenants / JTC Tenants (RTh) | **0.00 RTh** |
| | Total RTh (Chargeable to SIT) | **1,293,548.27 RTh** |
| **系统能效** | 月度能效 (EFF Monthly) | **0.581 kW/RT** |

---

### ❌ Copilot 返回数据（App Copilot 模拟调用结果）
当直接提问 Copilot：*“请找出202601.pdf的所有数据，并告诉我其具体内容”* 时，Copilot 输出了完全无关的**财务数据**，并声称上下文中不存在该文件：

> [!WARNING]
> **Copilot 答复摘要：**
> * “根据您提供的文档上下文，**不存在名为“202601.pdf”的文档**。上下文中最新的文档是 `202502.pdf`。”
> * 并给出了一张**完全虚构的 `202502.pdf` 的财务表格**：
>
> | 指标 | 本月数据 | 本年累计 | 去年同期 | 同比增长（%） |
> | :--- | :--- | :--- | :--- | :--- |
> | 产品销售收入 | 15,234.56 万元 | 30,456.78 万元 | 28,901.23 万元 | 5.38% |
> | 产品销售成本 | 9,876.54 万元 | 19,876.54 万元 | 18,543.21 万元 | 7.21% |
> | 净利润 | 1,176.45 万元 | 2,345.67 万元 | 2,456.78 万元 | -4.52% |
> | 员工人数 | 456 人 | 456 人 | 445 人 | 2.47% |

---

## 2. 差异产生原因分析（Why did this happen?）

造成这种极端差异（Copilot 找不到文件、且答复内容完全牛头不对马嘴）的原因在于 RAG 系统中存在的以下三个核心技术缺陷：

### 缺陷①：分块器 Bug 产生“空数据块”（Chunker Empty Table Bug）
在系统的文档处理流程中，`core/chunker.py` 试图检测并保护 PDF 中的表格。当它解析到 `=== TABLE DATA ===` 标识但未能成功匹配后续的行列结构时，它会把表格划分为一个单独的数据块。
这导致在 `metadata.pkl` 向量库索引中，包含大量内容仅为 `"=== TABLE DATA ==="`（长度仅 18 个字符）的**空数据块**。

### 缺陷②：向量检索相似度失真（Vector Store Retrieval Defect）
由于本地 Embedding 模型较小（`text-embedding-qwen3-embedding-0.6b`），对于包含大量无实际含义数据块的向量库，它的相似度匹配出现偏差：
* 当搜索 `"202601.pdf"` 或 `"202601"` 时，库中那些仅包含 `"=== TABLE DATA ==="` 字符串的无意义数据块（来源于 `202502.pdf`、`202412.pdf` 等）与查询词具有极高的词义相似度评分（`Score: 0.4974`）。
* 相反，真正属于 `202601.pdf` 的那个包含了 Jan/2026 监控数值的大数据块，因为字符较长且缺乏对应的中文字符匹配，其相似度得分较低（`Score: 0.3913`），**未能进入排名前 5 的检索上下文中**。

### 缺陷③：大语言模型上下文缺失与幻觉（LLM Hallucination）
由于检索器只返回了 5 个完全没有任何实际业务数据的 `"=== TABLE DATA ==="` 块，LLM 的输入上下文里根本没有 `202601.pdf` 的内容：
1. LLM 正确判断出“在当前给定的上下文中，找不到 `202601.pdf` 的具体数据”。
2. 但由于系统 Prompt 的强烈指令要求它“必须使用 ```markdown-canvas``` 返回完整的表格内容”，LLM 在看到上下文中只有 `202502.pdf` 来源的 `"=== TABLE DATA ==="` 块、且没有任何具体数据时，**完全凭空幻觉（Hallucinated）出了一套虚构的企业财务销售数据**。

---

## 3. 改进建议（How to Fix?）

1. **修复分块器规则 (`core/chunker.py`)**：
   丢弃或合并内容长度过短（例如小于 50 个字符）、仅包含结构修饰符的 Chunk，避免用无意义的字符串污染向量库。
2. **优化 Table 识别逻辑 (`core/pdf_loader.py` & `core/chunker.py`)**：
   在 PDF 提取时，如果未能识别出具体表格行列（`0 tables` 提取成功），不要在分块阶段产生隔离的 `=== TABLE DATA ===` 标记，而应将其作为普通文本进行段落分块。
3. **增加 Top-K 检索数或引入重排 (Reranker)**：
   提高默认 `top_k`（例如从 5 提高到 15），使得即使有干扰块，真实的文档块也能进入重排阶段，由 Reranker 进行二次筛选，把最贴合的 `202601.pdf` 块挑选出来。
